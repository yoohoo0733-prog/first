{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd /home/user/first && git status --porcelain | grep -q . && git add -A && git commit -m \"Auto-commit: changes after fix\" && git push -u origin \"$(git rev-parse --abbrev-ref HEAD)\" 2>&1 || true",
            "timeout": 30,
            "statusMessage": "Committing and pushing changes..."
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(git push *)",
      "Bash(git status *)",
      "Bash(git rev-parse *)"
    ]
  }
}
